# Workers Layer

Place Web Worker, Service Worker, or Shared Worker entry points and related logic in this directory.

Workers should communicate with the UI through message passing interfaces and remain decoupled from React-specific code.
