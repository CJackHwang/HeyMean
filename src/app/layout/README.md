# App Layout Layer

Place top-level shell and layout components here if you add them in the future, such as:

- AppShell
- MainLayout
- GlobalHeader
- GlobalSidebar

Keep layouts minimal and delegate business logic to features and widgets.
